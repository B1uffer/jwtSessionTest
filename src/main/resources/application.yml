server:
  port: 8081

spring:
  application:
    name: demo1
  datasource:
    url: jdbc:h2:mem:jwtSessionTest
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console

jwt:
  key: ${JWT_SECRET_KEY}
  access-token-expiration-minutes: 30
  refresh-token-expiration-minutes: 420
